export interface MainConfig {
    color: string | null
    length: number | null
}

export interface MousePosition {
    x: number
    y: number
}

export interface RGBAColor {
    red: number
    green: number
    blue: number
    alpha: number
}